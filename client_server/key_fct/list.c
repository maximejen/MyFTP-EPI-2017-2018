/*
** EPITECH PROJECT, 2018
** my_ftp
** File description:
** list.c
*/

#include "../../include/my_ftp.h"

int list(struct client_data *cdata, char **cmd)
{
	UNUSED(cdata);
	UNUSED(cmd);
	cdata->ctn = 0;
	return (0);
}