/*
** EPITECH PROJECT, 2018
** my_ftp
** File description:
** dele.c
*/

#include "../../include/my_ftp.h"

int dele(struct client_data *cdata, char **cmd)
{
	UNUSED(cdata);
	UNUSED(cmd);
	CONTINUE = 0;
	return (0);
}