/*
** EPITECH PROJECT, 2018
** my_ftp
** File description:
** port.c
*/

#include "../../include/my_ftp.h"

int port(struct client_data *cdata, char **cmd)
{
	UNUSED(cdata);
	UNUSED(cmd);
	CONTINUE = 0;
	return (0);
}